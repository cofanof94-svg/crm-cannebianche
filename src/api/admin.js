const express = require('express');

// Stub minimo per il Task 7: la logica reale verrà implementata nel Task 8.
function createAdminRouter() {
  return express.Router();
}

module.exports = { createAdminRouter };
